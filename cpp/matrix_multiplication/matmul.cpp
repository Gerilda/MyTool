//
// Created by Anastasia on 06.10.2019.
//
#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;
/*
int main() {
    
    double** aMatrix, bMatrix, cMatrix;
    int row, col;
    std::cin >> row >> col;

    aMatrix = new double[row][col];
    for (int i = 0; i < row; ++i) {
        for (int j = 0; j < col; ++j) {
            for (int inner = 0; inner < 2; inner++) {
                cMatrix[row][col] += aMatrix[row][inner] * bMatrix[inner][col];
            }
        }
    }
    return 0;
}
*/